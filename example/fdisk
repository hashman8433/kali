# ubuntu ntfs config write permission
apt install -y ntfs-config

# mount ntfs disk  (centos)

# search ntfs disk volumn
fdisk -l

# create director
mkdir /mnt/sdb5

# mount ntfs disk
mount -t ntfs-3g /dev/sdb5 /mnt/sdb5
